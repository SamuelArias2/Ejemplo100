package org.admin.repositories;

import org.admin.entities.deviceassignment;
import org.admin.entities.user;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ideviceassignmentrepository extends JpaRepository <deviceassignment, Integer>{
}
